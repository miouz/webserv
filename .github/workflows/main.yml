name: mandatory tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: mandatory tests
      run: |
        ls -lR
        make re
      # run: make; ./tester/test m
      shell: bash
